{
  "name": "wander-around-frontend",
  "version": "0.1.0",
  "description": "Wander Around — desktop / web frontend for the v2 engine.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5173",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
